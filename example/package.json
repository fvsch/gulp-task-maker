{
  "name": "gtm-example",
  "private": true,
  "scripts": {
    "build": "gulp build",
    "watch": "gulp --series build watch",
    "debug": "GTM_DEBUG=1 gulp --tasks"
  },
  "dependencies": {
    "jquery": "^3.3",
    "normalize.css": "^8.0"
  },
  "devDependencies": {
    "@gtm-example/mincss": "./tasks/mincss",
    "@gtm-example/minjs": "./tasks/minjs",
    "gulp": "^4.0",
    "gulp-task-maker": "2.0.0-beta1"
  },
  "browserslist": [
    "last 2 versions",
    "not ie < 11"
  ]
}
